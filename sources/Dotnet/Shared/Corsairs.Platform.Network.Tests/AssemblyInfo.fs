module Corsairs.Platform.Network.Tests.AssemblyInfo

open Xunit

// Тесты поднимают TCP-слушателей на loopback, а свободный порт выбирается
// единственным доступным способом: занять порт 0, узнать номер, освободить.
// Между освобождением и повторным занятием порт может достаться соседней
// коллекции, поэтому параллельный прогон то ловит таймауты, то зависает
// целиком. Последовательный прогон занимает те же секунды и воспроизводим.
[<assembly: CollectionBehavior(DisableTestParallelization = true)>]
do ()
