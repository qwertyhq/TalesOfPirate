// POSIX-версия того, что на Windows живёт в SystemDialog.cpp.
//
// Оригинал на 570 строк — это диалог управления сервером: окна, GDI-отрисовка
// карты, обработчики сообщений. Ничего из этого на POSIX не существует, файл
// целиком исключён из сборки. Но кроме окон он содержит две вещи, нужные
// серверу всегда: несколько глобальных переменных и SystemReport(), который
// помимо обновления полей диалога разбирает очередь логов.
//
// Здесь — только эта содержательная часть.

#include "App/GameApp.h"

// Координаты области просмотра карты в диалоге. На POSIX диалога нет, но
// lua_gamectrl.cpp присваивает им значения через extern-объявления, поэтому
// определения обязаны существовать.
long g_lViewAtMapX = 0;
long g_lViewAtMapY = 0;
long g_lMouseAtMapX = 0;
long g_lMouseAtMapY = 0;
BOOL g_bShowView = 0;

void SystemReport(DWORD /*dwTimeParam*/) {
	// Единственное, что делает оригинал помимо работы с окнами: разбирает
	// накопленную очередь логов. Без этого вызова записи копятся в памяти.
	g_pGameApp->HandleLogList();
}
